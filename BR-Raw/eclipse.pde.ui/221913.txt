Bug 221913 – Provide mechanism to remove API tooling
It would be nice if there was a way to remove api tooling setup from a project (remove the builder and nature).
I suppose it could even be super-deluxe and recreate the component.xml file based on tags it finds (and removes).